sample:
  name: Espressif QEMU test
tests:
  sample.basic.blinky:
    platform_allow:
      - qemu
    tags:
      - LED
      - gpio
    filter: dt_enabled_alias_with_parent_compat("led0", "gpio-leds")
    depends_on: gpio
    harness: led
